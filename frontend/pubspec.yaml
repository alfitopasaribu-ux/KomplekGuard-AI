name: frontend
description: KomplekGuard AI - Aplikasi Keamanan dan Tanggap Darurat Lingkungan
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6
  http: ^1.1.0
  flutter_map: ^6.1.0
  latlong2: ^0.9.0
  geolocator: ^11.0.0
  shared_preferences: ^2.2.2
  intl: ^0.19.0
  google_fonts: ^8.1.0
  speech_to_text: ^7.4.0
  permission_handler: ^12.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true